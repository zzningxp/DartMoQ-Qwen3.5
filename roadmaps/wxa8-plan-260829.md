# WxA8 路线规划（2026-08-29）

> WxA16 阶段已进入维护态（混合 bit 权重 + fp16 激活 + fp16 Tensor Core 计算）。
> 本文件规划 **WxA8** 新路线：激活量化为 INT8，矩阵运算走 INT8 Tensor Core（INT32 累加），
> 反量化回 FP16 后做非线性。目标：在 ppl 可控前提下进一步提升推理速度。

---

## 一、技术方案

### 数据流（一层 MoE 为例）

```
上一层输出 (FP16)
    ↓
[分组旋转]  per-group QR 正交旋转（group_size=128，每 group 独立 seed）
    ↓                                      ← 已有，WxA16 也做这一步
[激活量化]  per-token per-group 对称量化 → INT8 + scale
    ↓                                      ← 必须在旋转之后
[矩阵乘法]  INT8 激活 × INT8 权重（码本查表到 INT8）→ INT32 累加（group 内）
    ↓
[反量化]   INT32 × act_scale[b,g]（已含 cb_step） × norms_gf[n,g] → FP32 累加 → FP16
    ↓
[非线性]    SiLU / MoE gate 等（FP16）
    ↓
[下一层]
```

### 核心要点

- **A8 = 参与矩阵运算的激活输入是 8-bit**（不是输出 8-bit，也不是激活函数 8-bit）
- 矩阵乘累加器 group 内是 **INT32**，出 group 转 FP32 总累加
  （现有 WxA16 是 fp16 累加，所以 WxA8 在累加侧是**精度升级**）
- 反量化后回到 **FP16**，所有非线性、归一化、路由都在 FP16 下做
- 权重仍然是混合 bit（1/2/4bit），kernel 内 unpack 后查 **INT8 码本**
- Triton 原生支持 `tl.dot(int8, int8, out_dtype=int32)`，
  **sm120 + Triton 3.5.0 实测走通**（见 §四）
- ⚠ **激活量化必须在分组旋转之后**，且 gate_up / down 的旋转不同，
  不存在"在 MoE 入口一次量化两边共用"

### 为什么是 WxA8（先不做 WxA4）

| 维度 | WxA8 | WxA4（int4 计算） |
|---|---|---|
| Triton 支持 | ✅ `tl.dot(int8,int8) → int32` 成熟（sm120 实测走通） | ❌ 无 WGMMA int4，需外部库或 inline PTX |
| 理论吞吐 | **2x FP16**（SM120 dense：FP16 419 TFLOPS / INT8 838 TOPS） | ~4x FP16（SM120） |
| ppl 退化 | 小（激活量化实测 relerr 0.65%） | 中（0.5-1.0 ppl，需更精细量化策略） |
| 工程难度 | 低-中（融合 kernel 已跑通，见 §四） | 高（20-40 人天，Machete 库或 CUTLASS） |
| 基础设施复用 | 高（量化框架、MoE 调度全部复用） | 中（需要重新处理 int4 packing） |

> ⚠ 修正：初版本表写的"WxA8 ~4x / WxA4 ~8x FP16"是错的。SM120 上
> INT8 是 FP16 的 **2x**（不是 4x），FP4 才是 4x。而且实测这些形状下裸
> `tl.dot` 只跑到 194 TFLOP/s（fp16 峰值 419），本来就没吃满 MMA，
> 所以 WxA8 的收益主要来自**输入 tile 访存减半**，不是 MMA 变快。

**结论**：WxA8 是最务实的下一步。Triton 原生支持，工程可控，精度风险低。
等 WxA8 跑通验证后，再考虑 WxA4（上 Machete 库或 CUTLASS）。

---

## 二、量化方案

### 激活量化

- **方式**：per-token **per-group** 对称量化（group 粒度 = 权重量化的 group_size = 128）
- **公式**：`x_int8 = round(x_rot / scale)`，`scale = max(abs(x_rot)) / 127`（按 group 取 max）
- **为什么是 per-group 而不是 per-token 整行**：激活进 kernel 前已按 group
  分别做过**独立的 QR 旋转**（每个 group 一个旋转矩阵，seed = seed_base + g_start），
  各 group 幅度不同；per-group scale 让每个 group 都吃满 int8 的 127 级。
  而且现有 kernel epilogue 本来就逐 group 乘 norm，加一个逐 group 的激活
  scale 不增加结构复杂度。
- **计算位置**：⚠ **必须在分组旋转之后**，不是"MoE forward 入口"。
  旋转是正交变换但按 group 独立进行，会改变每个 group 的幅度分布，
  scale 必须在旋转后的值上算。
  而且 **gate_up 和 down 用的是不同旋转**
  （down 的 seed 还带 expert 偏移，见 `triton_kernels.py:1306`），
  K 空间也不同（2048 vs 512）——**不存在"在 MoE 入口一次量化两边共用"的可能**。
  正确落点：
  - gate_up：融进 `_build_hoisted_rotations`（`bit_partitioned_moe.py:526`），
    每个 bit 旋转+量化一次，per-expert 只切片。顺带让 `gather_rotated`
    的字节数减半。
  - down：旋转 seed 依赖 expert，只能逐 expert 在 `batch_rotate_input` 之后量化。
- **实测精度**：per-group int8 的相对误差 0.65%（高斯下的理论值也是 0.66%），
  与 tile 配置无关。

### 权重量化

- 权重仍然是**混合 bit 分组量化**格式（与 WxA16 相同的 packing 方式）
- Kernel 内部：权重 unpack → 查 **INT8 码本**（而不是 fp16 码本）
- 码本转 INT8：`cb_i8 = round(codebook / max|codebook| * 127)`，
  把 `cb_step = max|codebook|/127` **折进激活 scale**（不是 norms）——
  这样 `norms_gf` 与 WxA16 共用同一份 fp16 buffer，零额外显存，
  也避免把 norms 乘小到 fp16 次正规区
- **低 bit 下这一步代价很小，现有 2bpw checkpoint 直接可用、无需重新量化**：

  | bit | 总 relerr | 码本额外贡献 |
  |---|---|---|
  | 1 | 0.00647 | ≈ 0 |
  | 2 | 0.00651 | ≈ 0 |
  | 4 | 0.00860 | ≈ 0.0056 |
  | **8** | **0.0128** | **≈ 0.011 ⚠** |

- ⚠ **8-bit 是例外，这是初版计划漏掉的风险**：8-bit 码本是 256 级
  **Lloyd-Max（非均匀，零点附近极密）**，映射到均匀的 int8 网格时零点附近
  多级会塌成同一个值，等效把 W8 降成约 **W6**。attention 当初正是因为敏感
  才留 8-bit，所以 attention 路径要走 WxA8 **必须先把 8-bit 量化改成
  均匀对称码本**（高码率下均匀 vs Lloyd-Max 的 MSE 差距 < 0.25 dB，
  精度上基本免费，但需要重新量化 attention —— MoE 不受影响）。

### 反量化（矩阵乘后）

- `out_fp16 = out_int32 * act_scale[b,g] * norms_gf[n,g]`
  其中 `act_scale = amax/127 * cb_step`（旋转后按 group 取 max，见上），
  `norms_gf = norms / sqrt(group_size)`（与 WxA16 完全同一份 buffer）
- **两个 scale 都是 per-group 的，正好落在内层 dot 之外** —— 现有 WxA16
  kernel 本来就是 `total_acc += acc_g * norm_g`，把激活 scale 塞进同一个
  epilogue 即可，**数据流结构一行都不用改**。这是 WxA8 工程量远低于
  预估的根本原因。
- 累加器：group 内 INT32，出 group 转 FP32 总累加。
  注意现有 WxA16 是 **fp16 累加**（`out_dtype=tl.float16`），
  所以 WxA8 在累加侧其实是**精度升级**。

---

## 三、实施阶段（P 阶段）

### P0：基础设施 & 基线验证
- [x] WxA16 代码迁移到 `quantization/wxa16/`，目录结构就绪
- [x] 主流程 `--wxa16 / --wxa8` 开关框架（WxA8 先报未实现）
- [x] 激活量化参考实现（torch 版，per-token **per-group** symmetric int8）
      → `triton_kernels_a8.quantize_act_per_token_group`
- [x] 权重 INT8 码本预计算 → `triton_kernels_a8.build_int8_codebook`
- [x] INT8 全链路数值对拍工具 → `test/wxa8_common.py` + `test/test_wxa8_kernel.py`

### P1：WxA8 融合 kernel ✅ 已完成
> 原计划"先做 Dense 版验证可行性"这一步跳过了：MoE 的 group-first 融合
> kernel 本身就是 dense kernel，直接在真实形状上做即可，不需要额外的
> dense 中间产物。

- [x] 单 kernel：INT8 输入 × INT8 码本权重 → `tl.dot(int8)` → INT32 → 反量化 FP16
      → `turboquant_utils/triton_kernels_a8.py`
- [x] 独立的 INT8 tile 配置调优（**不能沿用 WxA16 的表**，见下）
- [x] 性能基准：对比 WxA16 同形状加速比
- [x] 数值对拍：与 fp32 参考解对比，12 组全通过
- [x] 切片一致性：行切片逐元素一致 / group 切片可加性 rel < 5.6e-4
- [x] SM 占用率 / 填充度分析
- [x] B 自适应盈亏交叉点扫描 → `WXA8_MIN_B = 256`

### P2：MoE 模块接入 ✅ 已完成
- [x] `quantization/wxa8/bit_partitioned_moe.py`：`WxA8BitPartitionedGroupMoE`
      继承 WxA16，复用全部存储/加载；`from_wxa16()` 原地切类（零拷贝）
- [x] 基类 forward 抽出 `_gate_up_matmul` / `_down_matmul` 两个覆盖点，
      重构前后 WxA16 输出逐元素一致（test_wxa8_moe.py 复验 + test_moe_integration 零回归）
- [x] 激活量化落点：gate_up 在 `_build_hoisted_rotations` 旋转后量化（预旋转
      的 bit 直接缓存 int8 + scale）；down 逐 expert 旋转后量化
- [x] 权重侧零改动：cb_step 折进激活 scale，`norms_gf` 与 WxA16 共用同一份
      fp16 buffer（省掉每层 fp32 副本，真实规模 40 层约 1GB）
- [x] MoE forward 数值对拍（对 WxA16 forward）：relerr 0.010~0.014，
      与 kernel 级对拍量级一致
- [x] B 分派问题实测后**否决**：小 B 绝对损失 ≤2.8us，双缓冲/重算旋转的
      成本比这更大 → 无条件走 A8（`wxa8_is_profitable` 保留给独立场景）
- [x] **rotate + quantize 融成一个 Triton kernel**（`rotate_quantize_fused`，
      对拍 maxd=1 零超差；hoist 场景 0.28ms vs 两段式 5.23ms = 18.75x，
      中间结果不落地，消除 fp32 中间态显存峰值）
- [x] 主流程接线：`qwen35_quant_io.convert_model_to_wxa8` +
      `load_quantized_model(inference_quant_mode="wxa8")`；
      `eval_qwen35.py` / `run_qwen35.py` 的 `--inference-quant-mode wxa8` 已放行
      （test_quant_io.py 扩展了转换后 forward 对拍，relerr=0.01336）
- [ ] 每层显存释放检查（int8 缓冲 + fp16 缓冲共存期要及时释放）
- [x] 端到端 eval：ppl 验证 + 分层 profile（见下方「端到端实测」）

### P3：attention 路径 ✅ 已完成
- [x] **前置**：8-bit 均匀码本（`quantize.py` 的 `uniform_codebook` 参数，
      `cb[i]=(i-128)*(3.5/128)`，indices 直接映射为 int8 权重；
      `W8A16Linear` 默认启用；meta 记录 `codebook_type`）
- [x] bit=8 的 INT8 tile 配置补扫：attn 形状最优 `(128,128,128,8,3)`（~420 TOP/s）
- [x] `WxA8Linear`（`quantization/wxa8/linear.py`，gf 惰性转换 + rotate-quantize
      融合 + IDENTITY_CB 免查表）；实测 **2.63x / 2.85x** vs WxA16Linear（qkv/o 形状）
- [x] `convert_model_to_wxa8` 扩展：8-bit 均匀码本 linear 转 WxA8Linear；
      **安全阀**：Lloyd-Max 码本（含旧 checkpoint）保持 W8A16 + 打印警告
- [x] 数值对拍：均匀码本下权重侧零误差，总 relerr 0.0065（= 激活量化单独的量）
- [x] 重量化 checkpoint（260831-u8，MoE 与 260824 同型）+ 端到端 eval
- [x] delta-rule / gate 保持 FP16（本轮未动，符合设计）
- [x] **端到端实测：attention 贡献 wiki -13.2% / c4 -16.8%**（见 §四 端到端实测）

> 决策记录（回答"能不能和 MoE 用同一套"）：不能。MoE 的 1/2/4-bit 码本只有
> 2/4/16 个值，加载期转 int8 无损；8-bit 的 256 级 Lloyd-Max 非均匀码本塞进
> int8 均匀网格会塌掉 61 级（实测 256→195，等效约 7.6-bit）。备选路线 fp8
> e4m3 实测三头全输：kernel 慢 30%（403 vs 526 TOPS）、激活误差 4 倍
> （2.57% vs 0.65%）、码本误差 5 倍（2.65% vs 0.53%）。均匀码本 + 重量化是
> 唯一全局最优（spike: test/test_wxa8_attn_fp8_spike.py）。

### P4：端到端接入 & 调优 ✅ 已完成
- [x] 完整模型加载 + WxA8 路径跑通推理（40 层 WxA8BitPartitionedGroupMoE + 310 个 linear）
- [x] ppl 验证：wiki +0.0022 / c4 **-0.003（好于 WxA16 基线）**
- [x] 速度评估：端到端 **wiki -19.3% / c4 -22.4%**（vs WxA16，见 §四「端到端实测」）
- [x] 分层 profile：MoE 贡献 -7.0%/-6.7%；attention 贡献 -13.2%/-16.8%；
      per-expert 循环占 MoE wall ~80% 的新发现

### P5：进阶优化（P4 之后，2026-09-02 细化）

> **现状基线**（P4 完成后）：端到端 wiki 46.95s / c4 68.79s，ppl 持平。
> MoE forward wall 中 triton kernel 仅占 ~20%，per-expert Python 循环占 ~80%
> （gather / rotate_quant / launch / silu / scatter 的串行开销）。
> 下一步核心思路：**减少 per-expert 循环内的独立 launch 与张量读写**。

#### P5-1：gate_up kernel 内融合 SiLU×up ❌ 已否决（实测反而变慢）
- **问题**：`bit_partitioned_moe.py:859` 的 `F.silu(gate_out) * up_out` 是独立的
  torch 操作，需要先把 gate_up 的完整 fp16 输出写回显存，再读一遍做 silu。
  gate_up 输出 N=1024，per-expert B=2048，约 4MB 显存流量 × 256 expert = 1GB/层。
- **方案**：独立 fusion kernel `_wxa8_gate_up_silu_kernel_gf`，同时加载 gate
  和 up 两套权重，在 epilogue 做 `out = silu(gate_acc) * up_acc`，输出只有
  inter_size 列，中间结果不落地。
- **实测结果（2026-09-02，RTX 5090，bit=2 large）**：
  - separate（gate_up kernel + torch silu）：0.050 ms
  - fused kernel：0.084 ms
  - **speedup = 0.60x（反而慢 40%）**
- **原因分析**：
  - silu 本身极快（element-wise fp16，~6us），收益上限本来就很低
  - fusion kernel 每个 CTA 要加载两套权重 tile + 做两次 tl.dot，
    寄存器压力翻倍、权重 tile 加载量翻倍，单 CTA 时间从 ~45us → ~84us
  - 虽然 CTA 数减半（256 → 128），但总时间还是涨了
- **结论**：**不值得做**。silu 的计算量太小，合进 matmul kernel 得不偿失。
  类似的小计算量 element-wise op 都不适合和 matmul 融合——matmul 的
  瓶颈在 MMA 和权重加载，不是显存带宽，省一次读写省不了多少。
- **代码状态**：kernel 已写（`triton_kernels_a8.py` 的
  `_wxa8_gate_up_silu_kernel_gf` + `wxa8_gate_up_silu_fused_gf`），
  未接入主流程，保留备查。测试脚本 `test/test_wxa8_silu_fusion.py`。

#### P5-2：scatter 融合进 down kernel ⚠️ 待评估（收益有限、复杂度中等）
- **问题**：`bit_partitioned_moe.py:886-887` 的 `expert_out.mul_` +
  `index_add_` 是两步独立操作，`expert_out` 完整 fp16 输出写完再读做 scatter。
- **实测开销（2026-09-02，B=2048 H=2048）**：
  - mul + index_add_ 合计 **0.027 ms / expert**
  - 256 expert 合计 ~6.9ms，占 MoE forward wall（118ms）的 **~5.8%**
- **方案**：down matmul kernel 的 epilogue 里直接做：
  `final_hidden_states[exp_token_idx[b], n] += out[b,n] * expert_weight[b]`
  需要给 kernel 传 final_hidden_states 指针 + exp_token_idx + expert_weights。
- **预期收益**：MoE wall -3~5%（上限 ~6%，实际有 overhead 达不到满值）。
- **难点**：
  - 输出从顺序写入变成按 index 随机写，缓存效率下降
  - exp_token_idx 内是否有重复需确认（理论上无重复，但需验证）
  - 工程复杂度比 P5-1 高（index 寻址、atomic 正确性等）
- **状态**：暂不实现。先做 P5-3（零风险、低成本），如果后续还有优化空间再回来评估。

#### P5-3：WxA8 tile 配置精细化 autotune ✅ 已完成（当前配置已是最优）
- **问题**：目前 WxA8 的 tile 配置（BLOCK_B=256, BLOCK_N=32, BLOCK_K=128,
  warps=4, stages=2）是 bit=2 + B=2048 + gate_up 的配置，其他场景（1-bit /
  4-bit / 小 B / down 方向）可能不是最优。
- **方法**：用 `test/test_wxa8_tune.py` 做全量网格搜索（162 配置 × 2 方向 × 2 档 B），
  并写了 `test/test_wxa8_cfg_compare.py` 做精确验证。
- **实测结果（2026-09-02，RTX 5090，bit=2 large）**：
  - gate_up：当前 (256,32,128,4,2) = 43.5us，**第 1 名**（已最优）
  - down：当前 (256,32,128,4,2) = 27.3us，**第 1 名**（已最优）
- **结论**：**当前配置已是全局最优**，P5-3 无提升空间。
  注意：`test_wxa8_tune.py` 测出的绝对值偏高（50us vs 43.5us），相对排名
  也有偏差；最终以更精确的对比测试为准。
- **可探索但预期低**：BLOCK_B=512、warps=2、num_stages=1 等更极端参数，
  从趋势看大概率不会更好（BLOCK_B=256 已是最优，再大可能尾波浪费更严重）。

#### P5-4：多 expert 合并 grouped kernel ⚠️ 待评估（WxA16 时代被否决，WxA8 下重评）
- **历史教训（WxA16，2026-08-26）**：分支 `opt-p5-2` 实现了
  `moe_gate_up_grouped_gf_v2` / `moe_down_grouped_gf_v2`，eval 从
  269.4s → 288.6s（变慢 7%），已决定不重测、未并入 main。
  推测原因：单 expert B=2048 已吃满 GPU，合并不提升填充度反而增加调度开销；
  expert 间负载不均衡导致尾波浪费；3D grid 按 expert 并行方案被证伪。
- **WxA8 下重评的理由**：
  - kernel 变快（1.52x），Python 循环占比从 ~70% → ~80%，减少 launch 的
    相对收益更高
  - rotate_quantize_fused + silu + scatter 都可以一起合并，减少的不只是
    matmul launch
  - 但 **反方理由也很强**：per-expert B=2048 时 gate_up 只有 256 CTAs / 1.51 波，
    合并 8 个 expert 变 2048 CTAs / 16 波——单 expert 的填充度已经不错，
    合并的边际收益可能被调度开销吃掉
- **评估方法（先算清楚再决定做不做）**：
  1. 先完成 P5-1 / P5-2 / P5-3，看 per-expert 循环还剩多少占比
  2. 用 CUDA event 精确拆解 per-expert 循环各阶段的时间（gather / rot_quant /
     gate_up / silu / down / scatter / python_overhead）
  3. 如果剩余瓶颈仍是 launch 数 × 固定开销，再考虑 grouped 方案
  4. 参考 `opt-p5-2` 分支的 `moe_gate_up_grouped_gf_v2` 实现思路，但要适配
     WxA8 的 int8 激活 + int8 码本 + rotate_quantize_fused 融合
- **难度**：中高。需要写新的 grouped kernel，处理 expert 间负载均衡，
  适配 WxA8 的数据流。如果做，第一阶段先做同一 bit 内所有 expert 的合并
  （gate_up 一个 kernel + down 一个 kernel），bit 间保留外层循环。

#### P5-5（远期）：量化链上的更多 fusion
- 残差加、rms_norm 融进量化路径（风险较高，涉及激活函数以外的层）
- per-channel 权重量化（精度 vs 性能 trade-off）



---

## 四、实测结果（2026-08-29 ~ 08-30，RTX 5090 / SM120 / Triton 3.5.0）

形状取自真实 eval（per-expert B = T×top_k/num_experts = 65536×8/256 = 2048）：
gate_up N=1024 K=2048，down N=2048 K=512，group_size=128。

### 融合 kernel 加速比（vs WxA16 同形状）

| 方向 | bit | WxA16 | WxA8 | 加速比 |
|---|---|---|---|---|
| gate_up | 1 | 65.1 us | 40.7 us | **1.60x** |
| gate_up | 2 | 71.1 us | 44.2 us | **1.61x** |
| gate_up | 4 | 103.2 us | 58.0 us | **1.78x** |
| down | 1 | 34.8 us | 25.9 us | 1.35x |
| down | 2 | 38.9 us | 28.0 us | **1.39x** |
| down | 4 | 55.3 us | 33.1 us | 1.67x |

bit=2 主力场景两个 kernel 合计 **109.9 → 72.2 us（1.52x）**。
按 backlog:207-215 的 CUDA event 口径（kernel 占 MoE forward 约 66%），
当时预期 MoE 层省约 23%；**实际端到端测出 MoE wall 省 10.6~11.1%**
（见下方「端到端实测」）—— 差距的原因是 kernel 只占 MoE wall 的约 20%，
详见端到端实测的注意点 3。

### INT8 的最优 tile 与 WxA16 完全不同 —— 必须单独调

沿用 WxA16 的配置只有 1.24x，重调后是 1.61x，**差 20%**：

| | WxA16 最优 | WxA8 最优 |
|---|---|---|
| gate_up large | (64, 128, 32, w4, s4) | **(256, 32, 128, w4, s2)** |
| down large | (64, 128, 128, w4, s2) | **(256, 32, 128, w4, s2)** |

规律：BLOCK_K=128（= group_size，内层只跑一次）与 BLOCK_N=32 全场景最优。
INT8 权重 tile 加载便宜，N 切窄换来更大的 BLOCK_B 更划算。

### B 盈亏交叉点与分派否决

盈亏交叉点在 **B ≈ 256**（更细的扫描见下表）：

| B | 8 | 32 | 64 | 128 | 256 | 1024 | 2048 |
|---|---|---|---|---|---|---|---|
| gate_up | 0.89x | 0.94x | 0.96x | 1.00x | 1.05x | 1.78x | 1.60x |
| down | 0.92x | 0.96x | 1.00x | 1.04x | 1.07x | 1.38x | 1.38x |

小 B 下 kernel 远没吃满（B=128 时填充仅 37.6%，0.38 波），INT8 省下的
访存与 MMA 换不回多加载两组 scale 的成本。

**但 B 自适应分派最终被否决**：小 B 区间的绝对损失最多 2.8us/调用
（B=8 时 gate_up），而大 B 能省 26.6us；为小 B 维护 fp16 缓冲
（每 bit 约 128MB）或逐 expert 重算旋转（额外 launch ~5us）都比
直接吃掉那 ≤2.8us 更亏 → **MoE 模块无条件走 A8**。
`WXA8_MIN_B=256` / `wxa8_is_profitable()` 保留给独立调用场景
（同时持有 fp16 与 int8 两份激活时）。

### 顺带发现的 WxA16 问题（不属于 WxA8，另记）

WxA16 在 B=256→384 处有性能悬崖：25.7 us → 59.3 us（工作量只多 1.5x，
耗时涨 2.3x）。原因是 `_B_THRESHOLD_SMALL=256` 一过就切到 large 档配置
(64,128,32,w4,s4)，而该配置在中等 B（384~1024）区间明显不合适。
**这是 WxA16 侧一个独立的优化机会**，建议补一档 "medium" 配置。

### GPU 填充度

| 场景 | grid | 波次 | 整体填充 |
|---|---|---|---|
| gate_up B=2048 | 256 CTAs | 1.51 | 75.3%（尾波仅 51%） |
| down B=2048 | 512 CTAs | 3.01 | 75.3% |

gate_up 的 1.51 波是个短板 —— 尾波只用了一半 SM。但配置扫描显示
256-CTA 的配置仍是最快的，说明当前受限于单 CTA 内的效率而非并行度。

### rotate+quantize 融合 kernel（P2 补充实测）

两段式（bmm 旋转 + 单独量化扫一遍）在真实规模下量化那遍是主体开销
（hoist B=65536：bmm 1.14ms，量化单独 4.09ms），且要过一遍 fp32 中间态。
融合后旋转结果全部留在寄存器，x 只读写各一趟：

| 场景 | 两段式 | 融合 | 加速比 |
|---|---|---|---|
| gate_up hoist B=65536 G=16 | 5.23 ms | **0.28 ms** | **18.75x** |
| down per-expert B=2048 G=4 | 0.06 ms | 0.01 ms | 4.57x |
| down 小 expert B=512 G=4 | 0.06 ms | 0.01 ms | 4.52x |

数值：与两段式逐元素对拍 maxd=1（int8 取整粒度内），scale relerr 8e-4
（fp16 存储精度）。融合 kernel 与 `batch_rotate_input` 的旋转约定完全一致
（`x @ P^T`），旋转矩阵直接复用 `generate_batch_rotation_matrices` 的缓存。

实现要点（容易写错的地方，都犯过并修了）：
1. **旋转方向**：必须算 `x @ P^T`（`acc = dot(x_tile, trans(p_tile))`），
   用 `x @ P` 时 scale 会凑巧接近（max 分布相似）而数值全错 —— 这种错
   单看 relerr 会骗过对拍
2. **cb_step 只折进存出的 scale**：量化除法 `q = round(acc / scale_unf)`
   必须用未折的 `amax/127`，折进去会把 q 全体放大 cb_step⁻¹ 倍再被
   clamp 饱和成 ±127
3. **BLOCK_B 不能缩块**：`tl.dot` 要求 M ≥ 16，极小 expert（B<16）靠
   mask 丢掉多余行，不动态缩 BLOCK_B
4. **模块级：down 输出要 cast 回原激活 dtype**（接线阶段踩到）：
   WxA8 wrapper 固定产出 fp16，而真实模型激活是 bf16 —— 不 cast 的话
   `index_add_` 直接 dtype 不匹配报错（合成测试的 fp32 输入先暴露了它，
   真实模型上必炸）。cast 对齐 WxA16 wrapper 的既有语义。

### 端到端实测（sequential eval，同一台机器；MoE-only 2026-08-30，
全路径 2026-09-01 基于 260831-u8 checkpoint）

| 数据集 | WxA16（260824） | WxA8 MoE-only | **WxA8 全路径** | ppl（全路径） |
|---|---|---|---|---|
| wikitext2 | 58.18s | 54.09s（-7.0%） | **46.95s（-19.3%）** | 7.7966（+0.0022） |
| c4 | 88.62s | 82.71s（-6.7%） | **68.79s（-22.4%）** | 11.265（-0.003，好于 A16） |

attention 路径单独贡献 **wiki -13.2% / c4 -16.8%** —— 远超开工前按
Global Stats 预估的 ~2%（当时 attention 只占 forward wall 约 10%）。
教训：**层级 wall-clock 的 attn/moe 计时（patched forward 计时）会被
异步队列的阻塞-吸收效应严重低估**，attention 的真实 GPU 占比比墙钟计时
显示的大得多；backlog 的 GPU 口径（attention 32%）反而是更接近真相的
那个。评估"下一步做什么"时不能只信一种口径。

ppl 全路径基本持平（wiki +0.0022 / c4 -0.003），均匀码本对 attention
权重没有造成可见退化 —— 8-bit 冗余确实足够。

forward 内部拆分（MoE-only 阶段 Global Stats，WxA16 vs WxA8）：

| 运行 | forward | moe | attn |
|---|---|---|---|
| WxA16 wiki | 37.97s | 32.33s | 6.97s |
| WxA8 wiki | **34.60s** | **28.74s（-11.1%）** | 6.90s |
| WxA16 c4 | 65.16s | 60.45s | 5.51s |
| WxA8 c4 | **59.17s** | **54.02s（-10.6%）** | 5.97s |

三个注意点：
1. **第一次 WxA8 跑会被 JIT 编译污染**（wiki 首次 62.68s，重跑 54.09s；
   逐层曲线前 5 层/后 5 层 = 1.49 vs 干净的 1.09，且未改动的 attn 也慢 16%）。
   测量前必须先跑一次任何 wxa8 eval 把 Triton 磁盘缓存捂热。
2. **旧 checkpoint（Lloyd-Max 8-bit）跑 wxa8 时 attention 自动保持 W8A16**：
   ppl 会与 MoE-only 完全一致（7.7975 / 11.2717），这曾是"看起来没提升"的
   一次误判 —— 判断 attention 是否生效看转换日志行
   `Converted to WxA8: 40 MoE layers, 310 attention/shared linears`。
3. **下一个大杠杆在 per-expert 循环，不在 kernel 本体**（计时口径已核实：
   模块内部单次 forward 的 wall 0.118s 里 triton 只有 0.023s ——
   **约 80% 是 256 个 expert 的 Python 循环**（gather / 旋转量化 /
   launch / silu / scatter 的串行开销）。kernel 再快也挪不动这 80%。
   方向：P5-2 的多 expert 合并 grouped kernel（`moe_gate_up_grouped_gf`
   等已存在但未并入 main）值得在 rotation hoist + int8 的新前提下重评。

### 手工测试方法

```bash
# 对拍 + 性能 + SM 填充度（约 2 分钟）
conda run -n dart312 python test/test_wxa8_kernel.py

# 额外扫 B 的盈亏交叉点
conda run -n dart312 python test/test_wxa8_kernel.py --bits 2 --crossover

# rotate+quantize 融合 kernel 对拍 + 性能
conda run -n dart312 python test/test_wxa8_fused_quant.py

# MoE 模块级对拍（WxA8 vs WxA16 forward，含 A16 零回归复验）
conda run -n dart312 python test/test_wxa8_moe.py

# attention 路径：WxA8Linear 对拍 + 性能 + 安全阀
conda run -n dart312 python test/test_wxa8_linear.py

# P3 路线决策 spike（int8 vs fp8 裸 dot / 码本转换 / 激活量化误差）
conda run -n dart312 python test/test_wxa8_attn_fp8_spike.py

# WxA16 零回归（重构 forward 的守护测试）
conda run -n dart312 python test/test_moe_integration.py

# checkpoint round-trip + wxa8 加载路径（convert_model_to_wxa8 后 forward 对拍）
conda run -n dart312 python test/test_quant_io.py

# 端到端 eval（全模型，需要你手工跑；⚠ 测速前先跑一次捂热 JIT 缓存）
python eval_qwen35.py --load-quantized models/qwen3.5-2bpw-260824 --inference-quant-mode wxa8 --datasets wikitext2 c4 --sequential-eval
python eval_qwen35.py --load-quantized models/qwen3.5-2bpw-260824 --inference-quant-mode wxa16 --datasets wikitext2 c4 --sequential-eval

# 重新调 INT8 tile 配置表（约 210 秒全扫，输出可直接粘贴的表）
conda run -n dart312 python test/test_wxa8_tune.py
conda run -n dart312 python test/test_wxa8_tune.py --quick   # 只扫 bit=2

# 最初的可行性 spike（含裸 tl.dot 上限 / dequant 开销拆解 / attention 形状）
conda run -n dart312 python test/test_wxa8_dot_spike.py
conda run -n dart312 python test/test_wxa8_dot_spike.py --sweep
```

---

## 五、里程碑

| 里程碑 | 交付物 | 验收标准 | 状态 |
|---|---|---|---|
| P0 完成 | 目录结构 + 开关框架 + INT8 参考实现 | WxA16 路径不受影响 | ✅ |
| P1 完成 | WxA8 融合 kernel + INT8 调优表 | 对拍通过 + 性能优于 WxA16 | ✅ 1.52x（bit2 主力） |
| P2 完成 | MoE 模块接入 + forward 对拍 | MoE forward 与 WxA16 对拍通过 | ✅ relerr 0.010~0.014 |
| P3 完成 | attention 均匀码本 + WxA8Linear | 8-bit relerr 回到 1e-3 量级 | ✅ relerr 0.0065 |
| P4 完成 | 端到端 eval | ppl 退化可控 + 端到端加速 | ✅ -19.3%/-22.4%，ppl 持平 |

### 收益预期 vs 实测

开工前按 backlog 的 GPU 时间口径（attention 32% / MoE 15%）预估：
只做 MoE 端到端约 **3%**，加上 attention 约 **5~6%**。

**实测远好于预估：MoE-only -7.0%/-6.7%，全路径 -19.3%/-22.4%**。
两个反直觉的事实：
1. MoE 贡献比预估大：sequential eval 工作流下 MoE 占 forward wall 约 83%
   （backlog 的 15% 是另一套口径）
2. attention 贡献也比预估大（-13%/-17% vs 预估 ~2%）：层级墙钟计时
   （patched forward）被异步队列的阻塞-吸收效应严重低估了 attention 的真实
   开销，backlog 的 GPU 口径反而更接近真相

教训：**单一计时口径会给出 5 倍以上偏差的预估**；做"下一步做什么"的决策
前，用 CUDA event 的 GPU 口径和 wall-clock 口径交叉验证。
真正的下一杠杆仍是 per-expert 循环（占 MoE wall 80%）。

---

## 六、关键风险

1. ~~**ppl 退化超预期**~~ → ✅ 已消除：真实 ppl 实测 wiki +0.0031 / c4 +0.0037，
   几乎为零退化（计划预估 0.1~0.3，实测好一个数量级）。旋转对离群值的抑制
   （等价 QuaRot 思路）+ per-group int8（relerr 0.65%）按设计工作。
2. **8-bit 码本转 int8 有损**（🆕 P1 新发现，初版漏掉）：
   256 级 Lloyd-Max 非均匀码本塞进均匀 int8 网格，等效 W8 → W6，
   relerr 0.00075 → 0.0128。**不影响已完成的 MoE 路径**（低 bit 无损）。
   - 缓解：attention 的 8-bit 改用均匀对称码本（需重量化 attention，MoE 不受影响）
3. ~~**性能不达预期：小矩阵吃不满 INT8 Tensor Core**~~ → 已实测确认存在
   （B<256 时 A8 慢 0.89x~1.03x），但**分派方案被否决**：小 B 绝对损失
   ≤2.8us，双缓冲/重算旋转的成本比这更大 → 无条件走 A8（见 §四）
4. **数值对齐复杂**：INT8 量化的舍入模式影响对拍
   - 已处理：torch 侧 `round()` 是 round-half-to-even，与 Triton 默认一致；
     对拍 12 组全通过，relerr 与 tile 配置无关（说明没有配置相关的错误）
5. **WxA16 迁移引入回归**：移动文件导致 import 错乱
   - 已消除：forward 重构后逐元素一致（test_moe_integration rel_err=0.00e+00
     守护），且已跑完整 WxA16 eval 验证零回归
6. ~~**P2 显存**：fp16/int8 双缓冲~~ → 已消除：分派否决后只存 int8 一份；
   旋转+量化融合 kernel 同时消除了 fp32 中间态（两段式 per-forward 峰值
   约为 4.5x 激活大小）
7. **JIT 编译污染测量**（🆕 端到端阶段新发现）：首次 wxa8 eval 逐 expert
   形状边跑边编译，端到端可虚增 10%+（wiki 首跑 62.68s vs 重跑 54.09s）。
   - 缓解：测速前先用任意一次 wxa8 eval 捂热 Triton 磁盘缓存

---

## 七、仓库结构

```
quantization/
├── __init__.py
├── common/               # 公共工具（两边共用的 profile / autotune / profiler 等）
│   └── __init__.py
├── wxa16/                # WxA16 实现（从根目录移入）
│   ├── __init__.py
│   ├── bit_partitioned_moe.py
│   ├── dartmoq_backend.py
│   ├── linear.py
│   └── memory_stats.py
└── wxa8/                 # WxA8 实现（新建）
    ├── __init__.py
    ├── bit_partitioned_moe.py   # P2：复用 wxa16 的存储/加载，只换 forward
    └── linear.py                # P3：attention 路径
```

kernel 与模块分层沿用 WxA16 的既有约定 —— **模块在 `quantization/`，
Triton kernel 在 `turboquant_utils/`**：

```
turboquant_utils/
├── triton_kernels.py       # WxA16 融合 kernel（既有）
└── triton_kernels_a8.py    # WxA8 融合 kernel（P1 新增）
                            #   复用 triton_kernels 的 convert_to_group_first 等布局工具
test/
├── wxa8_common.py            # 测试公共工具：按真实量化管线构造用例
├── test_wxa8_kernel.py       # 对拍 + 切片一致性 + 性能 + SM 填充度 + 交叉点扫描
├── test_wxa8_tune.py         # INT8 tile 配置调优
├── test_wxa8_dot_spike.py    # 可行性 spike（裸 dot 上限 / dequant 拆解 / attention 形状）
├── test_wxa8_fused_quant.py  # rotate+quantize 融合 kernel 对拍 + 性能
├── test_wxa8_moe.py          # MoE 模块级对拍（WxA8 vs WxA16 forward）
└── test_quant_io.py          # 既有 round-trip 测试，扩展了 wxa8 加载路径检查
```

- `turboquant_utils/` 暂不迁移（等明确哪些是公共的再逐步拆）
- 主流程通过 `--inference-quant-mode wxa16 / wxa8` 切换（默认 `wxa16`）
- **权重 checkpoint 不需要分开存**：WxA8 的 packed 格式与 WxA16 完全相同，
  码本转 int8 是加载期算的（低 bit 近乎无损）。
  唯一例外是 P3 的 attention 均匀码本改造，那个需要重量化。
